namespace lib.core;

public enum LevelType
{
    TUTORIAL = 1,
    LEVEL1 = 2,
    LEVEL2 = 3,
    LEVEL3 = 4,
    LEVEL4 = 5,
    LEVEL5 = 6
}

public class Level
{
    // in this we configure what enemy hordes will come into the level
    // what interval they will spawn in 
    // and amount of rounds ( a round will contain different types of hordes could be the 
    // same or randomized)
}

public class HubWorld
{
    // Main world before a level starts
    // also the interface you unlock after tutorial level
    // this is the place where you build items and craft before the levels
}